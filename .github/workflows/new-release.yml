on:
  push:
    tags:
      - 'v*'

name: Build apps
env:
  APP_NAME: otxto

jobs:
 build-linux:
   name: 🐧 Create Linux App
   runs-on: ubuntu-latest
   steps:
     - uses: actions/checkout@v3
     - uses: subosito/flutter-action@v2
       with:
         channel: 'stable'
     - run: |
        sudo apt-get update -y
        sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev
     - run: flutter config --enable-linux-desktop
     - run: flutter build linux
     - run: ls
     - name: Zip Release
       uses: thedoctor0/zip-release@0.7.1
       with:
        type: 'zip'
        directory: 'build'
        filename: 'linux-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'
     - name: 📼 Release
       uses: softprops/action-gh-release@v1
       with:
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        files: |
           build/*
 build-window:
   name: 🖼️ Create Windows App
   runs-on: windows-latest
   steps:
     - uses: actions/checkout@v3
     - uses: subosito/flutter-action@v2
       with:
         channel: 'beta'
     - run: flutter config --enable-windows-desktop
     - run: flutter build windows
     - run: ls
     - name: Zip Release
       uses: thedoctor0/zip-release@0.7.1
       with:
        type: 'zip'
        directory: 'build'
        filename: 'windows-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'
     - name: 📼 Release
       uses: softprops/action-gh-release@v1
       with:
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        files: |
            build/windows/runner/Release/*
 build-mac:
    name: 🍏 Create Mac App
    runs-on: macos-11
    steps:
    - uses: actions/checkout@v3
    - uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        architecture: x64
    - run: flutter pub get
    - run: flutter config --enable-macos-desktop
    - run: flutter build macos
    - name: Zip Release
      uses: thedoctor0/zip-release@0.7.1
      with:
           type: 'zip'
           directory: 'build'
           filename: 'mac-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'
    - name: 📼 Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        files: |
            'mac-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'
 build-android:
    name: 🧑‍🎤 Create Android APK
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-java@v2
      with:
        distribution: 'zulu'
        java-version: '11'
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.7.7'
    - run: flutter pub get
    - run: flutter build apk
    - run: flutter build appbundle
     - name: Zip Release
       uses: thedoctor0/zip-release@0.7.1
       with:
        type: 'zip'
        directory: 'build'
        filename: 'android-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'
    - name: 📼 Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        files: |
            'android-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'
 build-ios:
    name: 🍎 Create IOS
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        architecture: x64
    - run: flutter pub get
    - run: flutter build ios --release --no-codesign
    - name: Zip Release
      uses: thedoctor0/zip-release@0.7.1
      with:
           type: 'zip'
           directory: 'build'
           filename: 'ios-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'
    - name: 📼 Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        files: |
            'ios-${{ env.APP_NAME }}-${{ github.ref_name }}.zip'

